export const productServiceStub = () => ({
  findAll: jest.fn(),
  findByOrder: jest.fn(),
  save: jest.fn(),
})

export const productRepositoryStub = () => ({
  findAll: jest.fn(),
  findByOrder: jest.fn(),
  save: jest.fn(),
})
